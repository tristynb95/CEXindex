// ========== BAKERY METADATA ==========
window.GAILS = window.GAILS || {};

window.GAILS.DEFAULT_BAKERY_META = {
  "Balham": { "r": "South Region", "o": "Safa Aden", "ll": [51.4437, -0.1536] },
  "Barnes": { "r": "South Region", "o": "Safa Aden", "ll": [51.4724, -0.2427] },
  "Battersea Square": { "r": "South Region", "o": "Safa Aden", "ll": [51.4791, -0.1574] },
  "Earlsfield": { "r": "South Region", "o": "Safa Aden", "ll": [51.4371, -0.1879] },
  "Putney": { "r": "South Region", "o": "Safa Aden", "ll": [51.4618, -0.2158] },
  "Southfields": { "r": "South Region", "o": "Safa Aden", "ll": [51.4455, -0.2006] },
  "Streatham Hill": { "r": "South Region", "o": "Safa Aden", "ll": [51.4370, -0.1215] },
  "Tooting Broadway": { "r": "South Region", "o": "Safa Aden", "ll": [51.4275, -0.1681] },
  "Wandsworth": { "r": "South Region", "o": "Safa Aden", "ll": [51.4558, -0.1924] },
  "Wimbledon": { "r": "South Region", "o": "Safa Aden", "ll": [51.4213, -0.2075] },
  "Wimbledon Broadway": { "r": "South Region", "o": "Safa Aden", "ll": [51.4188, -0.2033] },
  "Cobham": { "r": "South Region", "o": "New Ops Area", "ll": [51.3311, -0.4100] },
  "Epsom": { "r": "South Region", "o": "New Ops Area", "ll": [51.3363, -0.2684] },
  "Esher": { "r": "South Region", "o": "New Ops Area", "ll": [51.3691, -0.3671] },
  "Kingston": { "r": "South Region", "o": "New Ops Area", "ll": [51.4100, -0.3012] },
  "Surbiton": { "r": "South Region", "o": "New Ops Area", "ll": [51.3927, -0.3043] },
  "Teddington": { "r": "South Region", "o": "New Ops Area", "ll": [51.4243, -0.3303] },
  "Weybridge": { "r": "South Region", "o": "New Ops Area", "ll": [51.3698, -0.4570] },
  "Windsor": { "r": "South Region", "o": "New Ops Area", "ll": [51.4838, -0.6078] },
  "Windsor Castle": { "r": "South Region", "o": "New Ops Area", "ll": [51.4836, -0.6047] },
  "Bracknell": { "r": "South Region", "o": "Michelle Solomon", "ll": [51.4155, -0.7471] },
  "Farnham": { "r": "South Region", "o": "Michelle Solomon", "ll": [51.2145, -0.7998] },
  "Fleet": { "r": "South Region", "o": "Michelle Solomon", "ll": [51.2833, -0.8450] },
  "Godalming": { "r": "South Region", "o": "Michelle Solomon", "ll": [51.1854, -0.6148] },
  "Guildford": { "r": "South Region", "o": "Michelle Solomon", "ll": [51.2365, -0.5714] },
  "Petersfield": { "r": "South Region", "o": "Michelle Solomon", "ll": [51.0026, -0.9348] },
  "Woking": { "r": "South Region", "o": "Michelle Solomon", "ll": [51.3192, -0.5594] },
  "Wokingham": { "r": "South Region", "o": "Michelle Solomon", "ll": [51.4104, -0.8348] },
  "Bristol College Green": { "r": "South Region", "o": "Matt Church", "ll": [51.4534, -2.5990] },
  "Bristol Whiteladies Road": { "r": "South Region", "o": "Matt Church", "ll": [51.4646, -2.6044] },
  "Cheltenham": { "r": "South Region", "o": "Matt Church", "ll": [51.8987, -2.0784] },
  "Clifton Village": { "r": "South Region", "o": "Matt Church", "ll": [51.4576, -2.6210] },
  "Marlborough": { "r": "South Region", "o": "Matt Church", "ll": [51.4205, -1.7270] },
  "Newbury": { "r": "South Region", "o": "Matt Church", "ll": [51.4012, -1.3227] },
  "Salisbury": { "r": "South Region", "o": "Matt Church", "ll": [51.0688, -1.7954] },
  "Union Street, Bath": { "r": "South Region", "o": "Matt Church", "ll": [51.3791, -2.3601] },
  "Brighton": { "r": "South Region", "o": "Holly Crawley", "ll": [50.8225, -0.1372] },
  "Horsham": { "r": "South Region", "o": "Holly Crawley", "ll": [51.0632, -0.3235] },
  "Hove": { "r": "South Region", "o": "Holly Crawley", "ll": [50.8299, -0.1699] },
  "Lewes": { "r": "South Region", "o": "Holly Crawley", "ll": [50.8741, 0.0086] },
  "Reigate": { "r": "South Region", "o": "Holly Crawley", "ll": [51.2368, -0.2060] },
  "Seven Dials Brighton": { "r": "South Region", "o": "Holly Crawley", "ll": [50.8279, -0.1449] },
  "Sevenoaks": { "r": "South Region", "o": "Holly Crawley", "ll": [51.2762, 0.1874] },
  "Tunbridge Wells": { "r": "South Region", "o": "Holly Crawley", "ll": [51.1329, 0.2624] },
  "Worthing": { "r": "South Region", "o": "Holly Crawley", "ll": [50.8182, -0.3714] },
  "Brentford": { "r": "South Region", "o": "Elizabeta Zakolli", "ll": [51.4887, -0.3076] },
  "Chiswick": { "r": "South Region", "o": "Elizabeta Zakolli", "ll": [51.4951, -0.2653] },
  "East Sheen": { "r": "South Region", "o": "Elizabeta Zakolli", "ll": [51.4628, -0.2757] },
  "Kew": { "r": "South Region", "o": "Elizabeta Zakolli", "ll": [51.4771, -0.2851] },
  "Richmond": { "r": "South Region", "o": "Elizabeta Zakolli", "ll": [51.4614, -0.3055] },
  "St Margarets": { "r": "South Region", "o": "Elizabeta Zakolli", "ll": [51.4568, -0.3240] },
  "Twickenham": { "r": "South Region", "o": "Elizabeta Zakolli", "ll": [51.4480, -0.3315] },
  "Abbeville": { "r": "South Region", "o": "Corina Medina", "ll": [51.4573, -0.1446] },
  "Clapham Old Town": { "r": "South Region", "o": "Corina Medina", "ll": [51.4635, -0.1416] },
  "Gatwick": { "r": "South Region", "o": "Corina Medina", "ll": [51.1537, -0.1821] },
  "Herne Hill": { "r": "South Region", "o": "Corina Medina", "ll": [51.4548, -0.1022] },
  "Northcote Road": { "r": "South Region", "o": "Corina Medina", "ll": [51.4640, -0.1640] },
  "Birmingham New Street": { "r": "North Region", "o": "Kate Downes", "ll": [52.4783, -1.8989] },
  "Buxton": { "r": "North Region", "o": "Kate Downes", "ll": [53.2586, -1.9100] },
  "Leamington Spa": { "r": "North Region", "o": "Kate Downes", "ll": [52.2855, -1.5183] },
  "Macclesfield": { "r": "North Region", "o": "Kate Downes", "ll": [53.2602, -2.1238] },
  "Solihull": { "r": "North Region", "o": "Kate Downes", "ll": [52.4124, -1.7775] },
  "Stratford-upon-Avon": { "r": "North Region", "o": "Kate Downes", "ll": [52.1938, -1.7086] },
  "Berkhamsted": { "r": "North Region", "o": "Indre Mingailiene", "ll": [51.7618, -0.5639] },
  "Harpenden": { "r": "North Region", "o": "Indre Mingailiene", "ll": [51.8167, -0.3584] },
  "Hitchin": { "r": "North Region", "o": "Indre Mingailiene", "ll": [51.9466, -0.2734] },
  "Radlett": { "r": "North Region", "o": "Indre Mingailiene", "ll": [51.6879, -0.3128] },
  "Ruislip": { "r": "North Region", "o": "Indre Mingailiene", "ll": [51.5769, -0.4264] },
  "St Albans": { "r": "North Region", "o": "Indre Mingailiene", "ll": [51.7529, -0.3360] },
  "Watford": { "r": "North Region", "o": "Indre Mingailiene", "ll": [51.6564, -0.3961] },
  "Welwyn Garden City": { "r": "North Region", "o": "Indre Mingailiene", "ll": [51.8043, -0.2074] },
  "Barnet": { "r": "North Region", "o": "Graeme Conroy", "ll": [51.6484, -0.2016] },
  "Cambridge": { "r": "North Region", "o": "Graeme Conroy", "ll": [52.2053, 0.1218] },
  "Cambridge Market Hill": { "r": "North Region", "o": "Graeme Conroy", "ll": [52.2055, 0.1185] },
  "Cambridge Station Square": { "r": "North Region", "o": "Graeme Conroy", "ll": [52.1939, 0.1378] },
  "Ely": { "r": "North Region", "o": "Graeme Conroy", "ll": [52.3989, 0.2625] },
  "Golders Green": { "r": "North Region", "o": "Graeme Conroy", "ll": [51.5712, -0.1944] },
  "Mill Hill": { "r": "North Region", "o": "Graeme Conroy", "ll": [51.6145, -0.2382] },
  "Muswell Hill": { "r": "North Region", "o": "Graeme Conroy", "ll": [51.5903, -0.1445] },
  "Palmers Green": { "r": "North Region", "o": "Graeme Conroy", "ll": [51.6190, -0.1093] },
  "Temple Fortune": { "r": "North Region", "o": "Graeme Conroy", "ll": [51.5799, -0.1817] },
  "Crouch End": { "r": "North Region", "o": "Gabor Merza", "ll": [51.5847, -0.1233] },
  "Highgate": { "r": "North Region", "o": "Gabor Merza", "ll": [51.5718, -0.1457] },
  "Swains Lane": { "r": "North Region", "o": "Gabor Merza", "ll": [51.5672, -0.1484] },
  "Camden": { "r": "North Region", "o": "Erika Kulikova", "ll": [51.5390, -0.1432] },
  "Clifton Road": { "r": "North Region", "o": "Erika Kulikova", "ll": [51.5255, -0.1852] },
  "Ealing": { "r": "North Region", "o": "Erika Kulikova", "ll": [51.5130, -0.3078] },
  "Kensal Rise": { "r": "North Region", "o": "Erika Kulikova", "ll": [51.5339, -0.2171] },
  "Maida Vale": { "r": "North Region", "o": "Erika Kulikova", "ll": [51.5253, -0.1851] },
  "Primrose Hill": { "r": "North Region", "o": "Erika Kulikova", "ll": [51.5388, -0.1597] },
  "Queens Park": { "r": "North Region", "o": "Erika Kulikova", "ll": [51.5345, -0.2050] },
  "St Johns Wood": { "r": "North Region", "o": "Erika Kulikova", "ll": [51.5353, -0.1727] },
  "West Hampstead": { "r": "North Region", "o": "Erika Kulikova", "ll": [51.5490, -0.1884] },
  "Willesden Green": { "r": "North Region", "o": "Erika Kulikova", "ll": [51.5492, -0.2283] },
  "Archway": { "r": "North Region", "o": "Connie Williamson", "ll": [51.5651, -0.1347] },
  "Belsize Park": { "r": "North Region", "o": "Connie Williamson", "ll": [51.5523, -0.1638] },
  "Finsbury Park": { "r": "North Region", "o": "Connie Williamson", "ll": [51.5641, -0.1065] },
  "Hackney Castle": { "r": "North Region", "o": "Connie Williamson", "ll": [51.5460, -0.0567] },
  "Hampstead": { "r": "North Region", "o": "Connie Williamson", "ll": [51.5563, -0.1784] },
  "Kentish Town": { "r": "North Region", "o": "Connie Williamson", "ll": [51.5500, -0.1416] },
  "South End Green": { "r": "North Region", "o": "Connie Williamson", "ll": [51.5551, -0.1652] },
  "Stoke Newington": { "r": "North Region", "o": "Connie Williamson", "ll": [51.5607, -0.0783] },
  "Victoria Park": { "r": "North Region", "o": "Connie Williamson", "ll": [51.5353, -0.0386] },
  "Altrincham": { "r": "North Region", "o": "Chris Kral", "ll": [53.3837, -2.3564] },
  "Cheshire Oaks": { "r": "North Region", "o": "Chris Kral", "ll": [53.2723, -2.8769] },
  "Chester": { "r": "North Region", "o": "Chris Kral", "ll": [53.1924, -2.8961] },
  "Didsbury": { "r": "North Region", "o": "Chris Kral", "ll": [53.4082, -2.2330] },
  "King Street": { "r": "North Region", "o": "Chris Kral", "ll": [53.4797, -2.2434] },
  "Knutsford": { "r": "North Region", "o": "Chris Kral", "ll": [53.3024, -2.3740] },
  "Manchester Sale": { "r": "North Region", "o": "Chris Kral", "ll": [53.4247, -2.3226] },
  "Prestwich": { "r": "North Region", "o": "Chris Kral", "ll": [53.5344, -2.2875] },
  "Wilmslow": { "r": "North Region", "o": "Chris Kral", "ll": [53.3294, -2.2334] },
  "Banbury Gateway": { "r": "North Region", "o": "Bobby Holmes", "ll": [52.076677, -1.318543] },
  "Beaconsfield": { "r": "North Region", "o": "Bobby Holmes", "ll": [51.6077, -0.6437] },
  "Gerrards Cross": { "r": "North Region", "o": "Bobby Holmes", "ll": [51.5826, -0.5549] },
  "Henley": { "r": "North Region", "o": "Bobby Holmes", "ll": [51.5353, -0.8991] },
  "Jericho": { "r": "North Region", "o": "Bobby Holmes", "ll": [51.7595, -1.2613] },
  "Marlow": { "r": "North Region", "o": "Bobby Holmes", "ll": [51.5706, -0.7761] },
  "Reading": { "r": "North Region", "o": "Bobby Holmes", "ll": [51.4543, -0.9781] },
  "Summertown": { "r": "North Region", "o": "Bobby Holmes", "ll": [51.7726, -1.2646] },
  "Thame": { "r": "North Region", "o": "Bobby Holmes", "ll": [51.7486, -0.9778] },
  "Witney": { "r": "North Region", "o": "Bobby Holmes", "ll": [51.7853, -1.4851] },
  "Baker Street": { "r": "London Region", "o": "Sandra Cano Cardona", "ll": [51.5225, -0.1568] },
  "Great Portland Street": { "r": "London Region", "o": "Sandra Cano Cardona", "ll": [51.5241, -0.1437] },
  "Holland Park": { "r": "London Region", "o": "Sandra Cano Cardona", "ll": [51.5064, -0.2059] },
  "Marylebone Village": { "r": "London Region", "o": "Sandra Cano Cardona", "ll": [51.5196, -0.1534] },
  "Melcombe Street": { "r": "London Region", "o": "Sandra Cano Cardona", "ll": [51.5230, -0.1581] },
  "Notting Hill": { "r": "London Region", "o": "Sandra Cano Cardona", "ll": [51.5117, -0.2011] },
  "Paddington": { "r": "London Region", "o": "Sandra Cano Cardona", "ll": [51.5160, -0.1768] },
  "Paddington Square": { "r": "London Region", "o": "Sandra Cano Cardona", "ll": [51.5164, -0.1753] },
  "Seymour Place": { "r": "London Region", "o": "Sandra Cano Cardona", "ll": [51.5186, -0.1622] },
  "Westbourne Grove": { "r": "London Region", "o": "Sandra Cano Cardona", "ll": [51.5137, -0.1969] },
  "Bloomsbury": { "r": "London Region", "o": "Samuel Suris Costas", "ll": [51.5212, -0.1293] },
  "Brunswick Centre": { "r": "London Region", "o": "Samuel Suris Costas", "ll": [51.5233, -0.1249] },
  "Euston": { "r": "London Region", "o": "Samuel Suris Costas", "ll": [51.5283, -0.1338] },
  "Great Russell Street": { "r": "London Region", "o": "Samuel Suris Costas", "ll": [51.5185, -0.1245] },
  "Kings Cross": { "r": "London Region", "o": "Samuel Suris Costas", "ll": [51.5308, -0.1241] },
  "Kings Cross Station": { "r": "London Region", "o": "Samuel Suris Costas", "ll": [51.5320, -0.1233] },
  "Liverpool Street Station": { "r": "London Region", "o": "Samuel Suris Costas", "ll": [51.5178, -0.0821] },
  "London Bridge Station": { "r": "London Region", "o": "Samuel Suris Costas", "ll": [51.5045, -0.0870] },
  "Pentonville Road Kings Cross": { "r": "London Region", "o": "Samuel Suris Costas", "ll": [51.5305, -0.1162] },
  "Soho": { "r": "London Region", "o": "Samuel Suris Costas", "ll": [51.5136, -0.1340] },
  "St Pancras Station": { "r": "London Region", "o": "Samuel Suris Costas", "ll": [51.5312, -0.1240] },
  "Askew Road": { "r": "London Region", "o": "New Group Manager", "ll": [51.5041, -0.2326] },
  "Barons Court": { "r": "London Region", "o": "New Group Manager", "ll": [51.4902, -0.2134] },
  "Brook Green": { "r": "London Region", "o": "New Group Manager", "ll": [51.4984, -0.2172] },
  "Fulham Broadway": { "r": "London Region", "o": "New Group Manager", "ll": [51.4795, -0.1948] },
  "Parsons Green": { "r": "London Region", "o": "New Group Manager", "ll": [51.4734, -0.1986] },
  "Billericay": { "r": "London Region", "o": "Magda Walczak", "ll": [51.6264, 0.4188] },
  "Brentwood": { "r": "London Region", "o": "Magda Walczak", "ll": [51.6213, 0.3050] },
  "Chelmsford": { "r": "London Region", "o": "Magda Walczak", "ll": [51.7357, 0.4685] },
  "Epping": { "r": "London Region", "o": "Magda Walczak", "ll": [51.7018, 0.1134] },
  "Loughton": { "r": "London Region", "o": "Magda Walczak", "ll": [51.6491, 0.0619] },
  "South Woodford": { "r": "London Region", "o": "Magda Walczak", "ll": [51.5897, 0.0273] },
  "Walthamstow Village": { "r": "London Region", "o": "Magda Walczak", "ll": [51.5817, -0.0205] },
  "Wanstead": { "r": "London Region", "o": "Magda Walczak", "ll": [51.5729, 0.0307] },
  "Buckingham Palace Road": { "r": "London Region", "o": "Magda Miszkiewicz", "ll": [51.4961, -0.1430] },
  "Chelsea": { "r": "London Region", "o": "Magda Miszkiewicz", "ll": [51.4875, -0.1688] },
  "Cromwell Place": { "r": "London Region", "o": "Magda Miszkiewicz", "ll": [51.4944, -0.1775] },
  "Gloucester Road": { "r": "London Region", "o": "Magda Miszkiewicz", "ll": [51.4955, -0.1826] },
  "High Street Kensington": { "r": "London Region", "o": "Magda Miszkiewicz", "ll": [51.5013, -0.1929] },
  "Kensington Arcade": { "r": "London Region", "o": "Magda Miszkiewicz", "ll": [51.5011, -0.1909] },
  "Kings Road": { "r": "London Region", "o": "Magda Miszkiewicz", "ll": [51.4876, -0.1742] },
  "Orchard Place": { "r": "London Region", "o": "Magda Miszkiewicz", "ll": [51.4980, -0.1336] },
  "Pimlico": { "r": "London Region", "o": "Magda Miszkiewicz", "ll": [51.4898, -0.1352] },
  "Strand": { "r": "London Region", "o": "Magda Miszkiewicz", "ll": [51.5094, -0.1228] },
  "Cheapside": { "r": "London Region", "o": "George Austin", "ll": [51.5142, -0.0910] },
  "Cowcross Street": { "r": "London Region", "o": "George Austin", "ll": [51.5210, -0.1027] },
  "Exmouth Market": { "r": "London Region", "o": "George Austin", "ll": [51.5237, -0.1095] },
  "Holborn": { "r": "London Region", "o": "George Austin", "ll": [51.5175, -0.1196] },
  "Islington": { "r": "London Region", "o": "George Austin", "ll": [51.5362, -0.1025] },
  "Long Acre": { "r": "London Region", "o": "George Austin", "ll": [51.5129, -0.1239] },
  "Millennium Bridge": { "r": "London Region", "o": "George Austin", "ll": [51.5096, -0.0979] },
  "Old Street": { "r": "London Region", "o": "George Austin", "ll": [51.5258, -0.0877] },
  "Shaftesbury Avenue": { "r": "London Region", "o": "George Austin", "ll": [51.5128, -0.1305] },
  "Shoreditch": { "r": "London Region", "o": "George Austin", "ll": [51.5257, -0.0791] },
  "Spitalfields": { "r": "London Region", "o": "George Austin", "ll": [51.5196, -0.0729] },
  "Beckenham": { "r": "London Region", "o": "Elena Ilciuc", "ll": [51.4085, -0.0219] },
  "Blackheath": { "r": "London Region", "o": "Elena Ilciuc", "ll": [51.4663, 0.0095] },
  "Bromley": { "r": "London Region", "o": "Elena Ilciuc", "ll": [51.4066, 0.0142] },
  "Canary Wharf": { "r": "London Region", "o": "Elena Ilciuc", "ll": [51.5035, -0.0235] },
  "Crossrail Place": { "r": "South Region", "o": "Elena Ilciuc", "ll": [51.5064, -0.0175] },
  "Crystal Palace": { "r": "London Region", "o": "Elena Ilciuc", "ll": [51.4174, -0.0735] },
  "Dulwich": { "r": "London Region", "o": "Elena Ilciuc", "ll": [51.4487, -0.0885] },
  "East Dulwich": { "r": "London Region", "o": "Elena Ilciuc", "ll": [51.4532, -0.0745] },
  "Westfield Stratford": { "r": "South Region", "o": "Elena Ilciuc", "ll": [51.5432, -0.0077] },
  "Woolwich Station": { "r": "London Region", "o": "Elena Ilciuc", "ll": [51.4904, 0.0694] },
  "Bermondsey Street": { "r": "London Region", "o": "Andrea D'Epifanio", "ll": [51.4997, -0.0853] },
  "Blackfriars": { "r": "London Region", "o": "Andrea D'Epifanio", "ll": [51.5122, -0.1033] },
  "Elephant Park": { "r": "London Region", "o": "Andrea D'Epifanio", "ll": [51.4937, -0.1005] },
  "Greenwich": { "r": "London Region", "o": "Andrea D'Epifanio", "ll": [51.4782, -0.0038] },
  "Kennington": { "r": "London Region", "o": "Andrea D'Epifanio", "ll": [51.4884, -0.1065] },
  "Neo Bankside": { "r": "London Region", "o": "Andrea D'Epifanio", "ll": [51.5083, -0.1032] },
  "South Lambeth": { "r": "London Region", "o": "Andrea D'Epifanio", "ll": [51.4793, -0.1235] },
  "Southbank": { "r": "London Region", "o": "Andrea D'Epifanio", "ll": [51.5073, -0.1126] },
  "Southwark": { "r": "London Region", "o": "Andrea D'Epifanio", "ll": [51.5038, -0.0935] },
  "The Cut": { "r": "London Region", "o": "Andrea D'Epifanio", "ll": [51.5027, -0.1074] },
  "Camden Lock": { "r": "Other", "o": "Other", "ll": [51.5441, -0.1471] }
};

// ========== HELPER FUNCTIONS ==========
function _normalizeBakeryLookupName(value) {
  return String(value == null ? '' : value)
    .trim()
    .toLowerCase()
    .replace(/&/g, ' and ')
    .replace(/\bgail[\'’]?s\b/g, ' ')
    .replace(/\bbakery\b/g, ' ')
    .replace(/[\'’]/g, '')
    .replace(/[^a-z0-9]+/g, ' ')
    .trim()
    .replace(/\s+/g, ' ');
}

function _registerBakeryAliases(index, name) {
  var trimmed = String(name == null ? '' : name).trim();
  var normalized = _normalizeBakeryLookupName(trimmed);
  if (!trimmed || !normalized || index[normalized]) return;
  index[normalized] = trimmed;
}

function _buildBakeryAliasLookup() {
  var index = {};
  Array.prototype.slice.call(arguments).forEach(function (source) {
    Object.keys(source || {}).forEach(function (name) {
      _registerBakeryAliases(index, name);
      _registerBakeryAliases(index, "GAIL's " + name);
      _registerBakeryAliases(index, "GAIL's Bakery " + name);
    });
  });
  return index;
}

// Space-insensitive fallback: catches names that only differ from their canonical
// meta key by spacing/punctuation (e.g. "Black Friars" vs "Blackfriars") without
// needing a hand-maintained alias for every such variant.
function _collapseBakeryLookupName(value) {
  return _normalizeBakeryLookupName(value).replace(/\s+/g, '');
}

function _registerBakeryCollapsedAlias(index, name) {
  var trimmed = String(name == null ? '' : name).trim();
  var collapsed = _collapseBakeryLookupName(trimmed);
  if (!trimmed || !collapsed || index[collapsed]) return;
  index[collapsed] = trimmed;
}

function _buildBakeryCollapsedAliasLookup() {
  var index = {};
  Array.prototype.slice.call(arguments).forEach(function (source) {
    Object.keys(source || {}).forEach(function (name) {
      _registerBakeryCollapsedAlias(index, name);
      _registerBakeryCollapsedAlias(index, "GAIL's " + name);
      _registerBakeryCollapsedAlias(index, "GAIL's Bakery " + name);
    });
  });
  return index;
}

var _defaultBakeryAliasLookup = _buildBakeryAliasLookup(window.GAILS.DEFAULT_BAKERY_META);
var _bakeryMetaAliasLookup = _buildBakeryAliasLookup(window.GAILS.DEFAULT_BAKERY_META);
var _defaultBakeryCollapsedAliasLookup = _buildBakeryCollapsedAliasLookup(window.GAILS.DEFAULT_BAKERY_META);
var _bakeryMetaCollapsedAliasLookup = _buildBakeryCollapsedAliasLookup(window.GAILS.DEFAULT_BAKERY_META);

// Normalized-form aliases for names that can't be matched by the standard lookup.
// Keys are the output of _normalizeBakeryLookupName; values are canonical meta keys.
var _BAKERY_EXTRA_ALIASES = {
  "black friars": "Blackfriars",             // "GAIL's Black Friars" == "GAIL's Blackfriars"
  "the fire station southwark": "Southwark", // "GAIL's The Fire Station, Southwark" == "GAIL's Southwark"
  "college green": "Bristol College Green",
  "college green bristol": "Bristol College Green",
  "union bath": "Union Street, Bath",
  "south kensington": "Cromwell Place",
  "south kinsington": "Cromwell Place"
};

window.GAILS.normalizeBakeryLookupName = _normalizeBakeryLookupName;

// Resolution runs seven regexes over the name and is the innermost call in every
// region/ops filter, so the dashboard reaches it a few times per record per
// pass. The answer only depends on the name and the alias lookups, and those are
// rebuilt in exactly one place, so cache it and clear the cache there.
var _bakeryMetaKeyCache = Object.create(null);

window.GAILS.resolveBakeryMetaKey = function (name) {
  var trimmed = String(name == null ? '' : name).trim();
  if (!trimmed) return '';

  var cached = _bakeryMetaKeyCache[trimmed];
  if (cached !== undefined) return cached;

  var resolved;
  if ((window.GAILS.BAKERY_META && window.GAILS.BAKERY_META[trimmed]) ||
      (window.GAILS.DEFAULT_BAKERY_META && window.GAILS.DEFAULT_BAKERY_META[trimmed])) {
    resolved = trimmed;
  } else {
    var normalized = _normalizeBakeryLookupName(trimmed);
    var direct = _BAKERY_EXTRA_ALIASES[normalized] ||
      _bakeryMetaAliasLookup[normalized] || _defaultBakeryAliasLookup[normalized];
    if (direct) {
      resolved = direct;
    } else {
      var collapsed = normalized.replace(/\s+/g, '');
      resolved = _bakeryMetaCollapsedAliasLookup[collapsed] ||
        _defaultBakeryCollapsedAliasLookup[collapsed] || trimmed;
    }
  }

  _bakeryMetaKeyCache[trimmed] = resolved;
  return resolved;
};

window.GAILS.getBakeryMeta = function (name) {
  var key = window.GAILS.resolveBakeryMetaKey(name);
  if (!key) return null;

  // The live site directory can briefly lag behind newly opened bakeries. Keep
  // the built-in directory as a coordinate/ownership fallback so a valid site
  // does not disappear from maps just because it is absent from that payload.
  return (window.GAILS.BAKERY_META && window.GAILS.BAKERY_META[key]) ||
    (window.GAILS.DEFAULT_BAKERY_META && window.GAILS.DEFAULT_BAKERY_META[key]) ||
    null;
};

window.GAILS.getBakeryMapLabel = function (name) {
  var text = String(name == null ? '' : name).trim();
  if (!text) return '';
  if (/^gail[\'’]?s\b/i.test(text)) return text;

  var meta = window.GAILS.getBakeryMeta(text);
  if (meta && meta.dn) return meta.dn;

  var key = window.GAILS.resolveBakeryMetaKey(text);
  return key ? "GAIL's " + key : text;
};

window.GAILS.cloneBakeryMeta = function (meta) {
  var source = meta && typeof meta === 'object' ? meta : {};
  var cloned = {};
  Object.keys(source).sort().forEach(function (name) {
    var trimmedName = String(name == null ? '' : name).trim();
    if (!trimmedName) return;

    var canonicalName = window.GAILS.resolveBakeryMetaKey(trimmedName) || trimmedName;
    var entry = source[name] || {};
    var existing = cloned[canonicalName] || {};
    var fallback = window.GAILS.DEFAULT_BAKERY_META[canonicalName] || null;
    var displayName = entry.dn ? String(entry.dn).trim() : trimmedName;
    var entryR = entry.r ? String(entry.r).trim() : '';
    var entryO = entry.o ? String(entry.o).trim() : '';
    cloned[canonicalName] = {
      r: (entryR && entryR !== 'Unknown') ? entryR : ((existing.r && existing.r !== 'Unknown') ? existing.r : (fallback ? fallback.r : 'Unknown')),
      o: (entryO && entryO !== 'Unknown') ? entryO : ((existing.o && existing.o !== 'Unknown') ? existing.o : (fallback ? fallback.o : 'Unknown')),
      ll: Array.isArray(entry.ll) ? entry.ll : (existing.ll || (fallback && Array.isArray(fallback.ll) ? fallback.ll : null))
    };
    if (displayName && displayName !== canonicalName) {
      cloned[canonicalName].dn = displayName;
    } else if (existing.dn) {
      cloned[canonicalName].dn = existing.dn;
    }
  });
  return cloned;
};

window.GAILS.setBakeryMeta = function (meta) {
  var fallback = window.GAILS.DEFAULT_BAKERY_META || {};
  window.GAILS.BAKERY_META = window.GAILS.cloneBakeryMeta(meta && Object.keys(meta).length ? meta : fallback);
  _bakeryMetaAliasLookup = _buildBakeryAliasLookup(fallback, window.GAILS.BAKERY_META);
  _bakeryMetaCollapsedAliasLookup = _buildBakeryCollapsedAliasLookup(fallback, window.GAILS.BAKERY_META);
  // Cleared last, not first: cloneBakeryMeta above resolves names itself, against
  // the outgoing directory, so anything it cached has to be thrown away too.
  _bakeryMetaKeyCache = Object.create(null);
  // Everything else that memoises a *resolved* bakery name is stale for the
  // same reason. These are optional so pages that don't load those files (or
  // load them after this one) still work.
  if (typeof window.GAILS.invalidateVisitPeriodIndex === 'function') {
    window.GAILS.invalidateVisitPeriodIndex();
  }
  if (typeof window.GAILS.invalidateFocusDataset === 'function') {
    window.GAILS.invalidateFocusDataset();
  }
  if (typeof window.GAILS.onBakeryMetaChanged === 'function') {
    window.GAILS.onBakeryMetaChanged(window.GAILS.BAKERY_META);
  }
  return window.GAILS.BAKERY_META;
};

window.GAILS.getBakeryMetaSnapshot = function () {
  return window.GAILS.cloneBakeryMeta(window.GAILS.BAKERY_META);
};

function _normalizeRegionAssignmentKey(value) {
  return String(value == null ? '' : value).trim().replace(/\s+/g, ' ').toLowerCase();
}

// A region on cover has its Coffee Partner and Coffee Trainer named per ops
// area instead of once for the whole region — what happens while one of them
// has left and their patch is split between colleagues. Areas nobody covers,
// and every region not on cover, still fall back to the region's own pair. See
// js/region-assignments.js, where the admin maintains this.
function _normalizeRegionCover(value) {
  var source = value && typeof value === 'object' ? value : {};
  var areas = Array.isArray(source.areas) ? source.areas : [];
  return {
    enabled: source.enabled === true || source.enabled === 'true',
    areas: areas.map(function (entry) {
      var record = entry && typeof entry === 'object' ? entry : {};
      return {
        opsArea: String(record.opsArea || '').trim().replace(/\s+/g, ' '),
        coffeePartner: String(record.coffeePartner || '').trim(),
        coffeePartnerUid: String(record.coffeePartnerUid || '').trim(),
        coffeeTrainer: String(record.coffeeTrainer || '').trim(),
        coffeeTrainerUid: String(record.coffeeTrainerUid || '').trim()
      };
    }).filter(function (area) {
      return !!area.opsArea &&
        !!(area.coffeePartner || area.coffeePartnerUid ||
           area.coffeeTrainer || area.coffeeTrainerUid);
    })
  };
}

function _cloneRegionCover(cover) {
  return {
    enabled: cover.enabled,
    areas: cover.areas.map(function (area) {
      return {
        opsArea: area.opsArea,
        coffeePartner: area.coffeePartner,
        coffeePartnerUid: area.coffeePartnerUid,
        coffeeTrainer: area.coffeeTrainer,
        coffeeTrainerUid: area.coffeeTrainerUid
      };
    })
  };
}

// Coffee Partner and Coffee Trainer ownership is maintained once per region
// in the admin site directory. Keep a normalized lookup alongside the public
// snapshot so any bakery-facing view can resolve those names consistently,
// even when an uploaded workbook varies the region's case or spacing.
window.GAILS.setRegionAssignments = function (assignments) {
  var source = Array.isArray(assignments)
    ? assignments
    : Object.values(assignments && typeof assignments === 'object' ? assignments : {});
  var records = [];
  var lookup = {};

  source.forEach(function (record) {
    var region = String(record && record.region || '').trim().replace(/\s+/g, ' ');
    if (!region) return;
    var normalized = {
      region: region,
      coffeePartner: String(record && record.coffeePartner || '').trim(),
      coffeePartnerUid: String(record && record.coffeePartnerUid || '').trim(),
      coffeeTrainer: String(record && record.coffeeTrainer || '').trim(),
      coffeeTrainerUid: String(record && record.coffeeTrainerUid || '').trim(),
      cover: _normalizeRegionCover(record && record.cover)
    };
    records.push(normalized);
    lookup[_normalizeRegionAssignmentKey(region)] = normalized;
  });

  records.sort(function (a, b) { return a.region.localeCompare(b.region); });
  window.GAILS.REGION_ASSIGNMENTS = records;
  window.GAILS._regionAssignmentLookup = lookup;
  return records;
};

// Pass the bakery's ops area to get the pair who actually look after it. The
// two roles resolve independently, so a region can split its Coffee Partner
// patch while the same Coffee Trainer carries on across all of it.
window.GAILS.getRegionAssignment = function (region, opsArea) {
  var key = _normalizeRegionAssignmentKey(region);
  var record = key && window.GAILS._regionAssignmentLookup
    ? window.GAILS._regionAssignmentLookup[key]
    : null;
  if (!record) return null;

  var resolved = {
    region: record.region,
    coffeePartner: record.coffeePartner,
    coffeeTrainer: record.coffeeTrainer,
    coverEnabled: record.cover.enabled,
    partnerFromCover: false,
    trainerFromCover: false
  };
  if (!record.cover.enabled) return resolved;

  var areaKey = _normalizeRegionAssignmentKey(opsArea);
  var area = areaKey && record.cover.areas.filter(function (entry) {
    return _normalizeRegionAssignmentKey(entry.opsArea) === areaKey;
  })[0];
  if (!area) return resolved;

  if (area.coffeePartner || area.coffeePartnerUid) {
    resolved.coffeePartner = area.coffeePartner;
    resolved.partnerFromCover = true;
  }
  if (area.coffeeTrainer || area.coffeeTrainerUid) {
    resolved.coffeeTrainer = area.coffeeTrainer;
    resolved.trainerFromCover = true;
  }
  return resolved;
};

window.GAILS.getRegionAssignmentsSnapshot = function () {
  return (window.GAILS.REGION_ASSIGNMENTS || []).map(function (record) {
    return {
      region: record.region,
      coffeePartner: record.coffeePartner,
      coffeePartnerUid: record.coffeePartnerUid,
      coffeeTrainer: record.coffeeTrainer,
      coffeeTrainerUid: record.coffeeTrainerUid,
      cover: _cloneRegionCover(record.cover)
    };
  });
};

// Area Head Barista ownership is maintained once per ops area in the admin
// site directory. Ops area names are only guaranteed unique inside their own
// region, so the primary lookup is keyed on the pair. A second lookup on the
// ops area alone lets a caller that only knows the bakery's ops area resolve
// it too — but only while that name is unambiguous across regions, so a
// duplicated ops area name can never hand back the wrong region's barista.
window.GAILS.setOpsAreaAssignments = function (assignments) {
  var source = Array.isArray(assignments)
    ? assignments
    : Object.values(assignments && typeof assignments === 'object' ? assignments : {});
  var records = [];
  var lookup = {};
  var opsOnlyLookup = {};
  var ambiguousOpsAreas = {};

  source.forEach(function (record) {
    var opsArea = String(record && record.opsArea || '').trim().replace(/\s+/g, ' ');
    if (!opsArea) return;
    var region = String(record && record.region || '').trim().replace(/\s+/g, ' ');

    var baristas = (Array.isArray(record && record.baristas) ? record.baristas : [])
      .map(function (entry) {
        var value = entry && typeof entry === 'object' ? entry : {};
        return {
          name: String(value.name || '').trim(),
          uid: String(value.uid || '').trim(),
          homeBakery: String(value.homeBakery || '').trim()
        };
      }).filter(function (entry) { return !!(entry.name || entry.homeBakery); });

    // An ops area saved before it could hold more than one area head barista
    // reads as a single entry.
    if (!baristas.length && record && (record.areaHeadBarista || record.homeBakery)) {
      baristas = [{
        name: String(record.areaHeadBarista || '').trim(),
        uid: String(record.areaHeadBaristaUid || '').trim(),
        homeBakery: String(record.homeBakery || '').trim()
      }];
    }

    var named = baristas.filter(function (entry) { return !!entry.name; });
    var normalized = {
      region: region,
      opsArea: opsArea,
      baristas: baristas,
      // Flattened for the many read-only surfaces that just want to print who
      // looks after an ops area.
      areaHeadBarista: named.map(function (entry) { return entry.name; }).join(' · '),
      areaHeadBaristaUid: named.length === 1 ? named[0].uid : '',
      homeBakery: named.length === 1 ? named[0].homeBakery : ''
    };
    records.push(normalized);

    var opsKey = _normalizeRegionAssignmentKey(opsArea);
    lookup[_normalizeRegionAssignmentKey(region) + ' ' + opsKey] = normalized;
    if (Object.prototype.hasOwnProperty.call(opsOnlyLookup, opsKey)) {
      ambiguousOpsAreas[opsKey] = true;
    } else {
      opsOnlyLookup[opsKey] = normalized;
    }
  });

  Object.keys(ambiguousOpsAreas).forEach(function (key) {
    delete opsOnlyLookup[key];
  });

  records.sort(function (a, b) {
    return a.region.localeCompare(b.region) || a.opsArea.localeCompare(b.opsArea);
  });
  window.GAILS.OPS_AREA_ASSIGNMENTS = records;
  window.GAILS._opsAreaAssignmentLookup = lookup;
  window.GAILS._opsAreaOnlyAssignmentLookup = opsOnlyLookup;
  return records;
};

window.GAILS.getOpsAreaAssignment = function (opsArea, region) {
  var opsKey = _normalizeRegionAssignmentKey(opsArea);
  if (!opsKey) return null;
  var record = null;
  if (region && window.GAILS._opsAreaAssignmentLookup) {
    record = window.GAILS._opsAreaAssignmentLookup[
      _normalizeRegionAssignmentKey(region) + ' ' + opsKey
    ] || null;
  }
  if (!record && window.GAILS._opsAreaOnlyAssignmentLookup) {
    record = window.GAILS._opsAreaOnlyAssignmentLookup[opsKey] || null;
  }
  return record ? {
    region: record.region,
    opsArea: record.opsArea,
    baristas: record.baristas.map(function (entry) {
      return { name: entry.name, uid: entry.uid, homeBakery: entry.homeBakery };
    }),
    areaHeadBarista: record.areaHeadBarista,
    homeBakery: record.homeBakery
  } : null;
};

window.GAILS.getOpsAreaAssignmentsSnapshot = function () {
  return (window.GAILS.OPS_AREA_ASSIGNMENTS || []).map(function (record) {
    return {
      region: record.region,
      opsArea: record.opsArea,
      baristas: record.baristas.map(function (entry) {
        return { name: entry.name, uid: entry.uid, homeBakery: entry.homeBakery };
      }),
      areaHeadBarista: record.areaHeadBarista,
      areaHeadBaristaUid: record.areaHeadBaristaUid,
      homeBakery: record.homeBakery
    };
  });
};

window.GAILS.setRegionAssignments([]);
window.GAILS.setOpsAreaAssignments([]);
window.GAILS.setBakeryMeta(window.GAILS.DEFAULT_BAKERY_META);

window.GAILS.getBakeryRegion = function (b) {
  var meta = window.GAILS.getBakeryMeta(b);
  return meta ? meta.r : 'Unknown';
};
window.GAILS.getBakeryOps = function (b) {
  var meta = window.GAILS.getBakeryMeta(b);
  return meta ? meta.o : 'Unknown';
};

// ========== ROUTINE VISITS (for map tooltips + the full visit report) ==========
// Keyed by resolved bakery name -> the most recent routineVisits record (full
// record, including its Firebase key as .id), populated from the
// routineVisits Firebase node (see js/auth.js).
window.GAILS._lastVisitRecords = {};

window.GAILS.setLastVisitRecords = function (records) {
  window.GAILS._lastVisitRecords = records && typeof records === 'object' ? records : {};
};

window.GAILS.getLastVisitRecord = function (b) {
  var key = window.GAILS.resolveBakeryMetaKey(b) || b;
  return window.GAILS._lastVisitRecords[key] || null;
};

window.GAILS.getLastVisitDate = function (b) {
  var record = window.GAILS.getLastVisitRecord(b);
  return record ? record.date : null;
};

// Converts a routineVisits ISO date ("yyyy-MM-dd") to the dashboard's
// month label format ("MMM YY"), so it can be matched against
// state.selectedMonths / state.MONTHS.
function _isoDateToMonthLabel(dateStr) {
  var parts = String(dateStr || '').split('-');
  if (parts.length < 2) return null;
  var mi = parseInt(parts[1], 10) - 1;
  if (isNaN(mi) || mi < 0 || mi > 11 || !window.GAILS.MONTH_SHORT) return null;
  return window.GAILS.MONTH_SHORT[mi] + ' ' + parts[0].slice(-2);
}

// Every routine visit, folded down to the only two things the visit filters
// ask about: which bakery it belongs to, and which dashboard month it lands in.
//
// The maps below used to re-scan the whole routineVisits node once per bakery,
// so drawing a map cost (bakeries x visits) key resolutions — around 200
// bakeries against a node that grows by every visit ever logged. The answer
// only changes when the node does, so it is folded once per snapshot and then
// read as a lookup.
//
// Cached against the snapshot object itself: auth.js, bakery-profile.js and
// admin-page.js all replace `_allVisitsObj` wholesale on every Firebase update,
// so a new object is exactly the signal that the fold is stale. Anything that
// starts editing the node in place must call GAILS.invalidateVisitPeriodIndex().
var _visitPeriodIndexSource = null;
var _visitPeriodIndex = null;

window.GAILS.invalidateVisitPeriodIndex = function () {
  _visitPeriodIndexSource = null;
  _visitPeriodIndex = null;
};

function _buildVisitPeriodIndex(visits) {
  // Null-prototype so a bakery or month label that happens to spell an
  // Object.prototype member can't be mistaken for an entry.
  var index = Object.create(null);
  Object.keys(visits).forEach(function (id) {
    var v = visits[id];
    // Same guard the per-bakery scan used: a visit with no bakery or no date
    // cannot match any period, and never counted as "visited at all" either.
    if (!v || !v.bakery || !v.date) return;
    var key = window.GAILS.resolveBakeryMetaKey(v.bakery) || v.bakery;
    var entry = index[key];
    if (!entry) {
      entry = index[key] = { any: false, months: Object.create(null) };
    }
    entry.any = true;
    var label = _isoDateToMonthLabel(v.date);
    if (label) entry.months[label] = true;
  });
  return index;
}

function _getVisitPeriodIndex() {
  var visits = window.GAILS._allVisitsObj || {};
  if (_visitPeriodIndex && _visitPeriodIndexSource === visits) return _visitPeriodIndex;
  _visitPeriodIndex = _buildVisitPeriodIndex(visits);
  _visitPeriodIndexSource = visits;
  return _visitPeriodIndex;
}

// Whether bakery b has a logged routine visit falling within the given list
// of dashboard month labels (pass the current state.selectedMonths so map
// toggles can filter markers by the selected period). An empty month list
// means "has ever been visited".
window.GAILS.isBakeryVisitedInPeriod = function (b, months) {
  var key = window.GAILS.resolveBakeryMetaKey(b) || b;
  var entry = _getVisitPeriodIndex()[key];
  if (!entry) return false;
  var monthSet = months || [];
  if (!monthSet.length) return entry.any;
  for (var i = 0; i < monthSet.length; i++) {
    if (entry.months[monthSet[i]] === true) return true;
  }
  return false;
};

// ========== COLOUR MAPS ==========
window.GAILS.COL = { 'Top Performance': '#1E70C4', 'Above Average': '#1D9E5C', 'Below Average': '#C97F12', 'Low Performance': '#B22A24', 'No Data': '#B3AA99', 'Incomplete': '#B3AA99' };
window.GAILS.ABSCOL = { 'Exceeding': '#1E70C4', 'Meeting': '#1D9E5C', 'Approaching': '#C97F12', 'Below Standard': '#B22A24', 'No Data': '#B3AA99', 'Incomplete': '#B3AA99' };

// ========== BAND NAMES ==========
window.GAILS.BAND_NAMES = ['Top Performance', 'Above Average', 'Below Average', 'Low Performance', 'Incomplete', 'No Data'];
window.GAILS.ABS_BAND_NAMES = ['Exceeding', 'Meeting', 'Approaching', 'Below Standard', 'Incomplete', 'No Data'];

// Display labels for the Benchmark Score range behind each band. These are for
// showing users where the cut-offs fall — the authoritative banding is the
// threshold ladder in GAILS.ensureBands (js/cei.js); keep the two in step.
window.GAILS.ABS_BAND_RANGES = {
  'Exceeding': 'Benchmark Score 90–100',
  'Meeting': 'Benchmark Score 75–89',
  'Approaching': 'Benchmark Score 60–74',
  'Below Standard': 'Benchmark Score under 60',
  'Not Scored': 'No Benchmark Score for this period'
};

// ========== CEI WEIGHTS & BENCHMARKS ==========
window.GAILS.CEI_WEIGHTS = { nps: 0.20, ef: 0.20, dr: 0.20, fr: 0.20, time: 0.10, at: 0.10 };
window.GAILS.BENCHMARKS = { nps: 55, ef: 90, dr: 90, fr: 90, time: 70, at: 120 };
window.GAILS.BENCHMARK_FLOORS = { nps: 45, ef: 80, dr: 80, fr: 80, at: 125 };

// One shared RAG model for the league table, Focus review and KPI cards.
// Keeping these rules here prevents the same metric appearing with different
// colours in different parts of the dashboard.
window.GAILS.metricRagTone = function (metric, value) {
  if (value === null || value === undefined || isNaN(value)) return '';
  if (metric === 'n' || metric === 'nps' || metric === 'nc' || metric === 'nm' || metric === 'na') {
    return value >= 55 ? 'green' : value >= 45 ? 'amber' : 'red';
  }
  if (metric === 'dr' || metric === 'ef' || metric === 'fr' || metric === 'ov') {
    return value >= 90 ? 'green' : value >= 80 ? 'amber' : 'red';
  }
  if (metric === 'ts' || metric === 's2' || metric === 'time') {
    return value >= 70 ? 'green' : value >= 60 ? 'amber' : 'red';
  }
  if (metric === 'o5') return value <= 1 ? 'green' : value < 2.5 ? 'amber' : 'red';
  if (metric === 'at') {
    return value <= window.GAILS.BENCHMARKS.at ? 'green' : value < window.GAILS.BENCHMARK_FLOORS.at ? 'amber' : 'red';
  }
  return '';
};

window.GAILS.metricRagColor = function (metric, value) {
  var tone = window.GAILS.metricRagTone(metric, value);
  return tone ? 'var(--' + tone + ')' : 'var(--muted)';
};

window.GAILS.metricRagStyle = function (metric, value) {
  var tone = window.GAILS.metricRagTone(metric, value);
  return tone ? ' style="color:var(--' + tone + ')"' : '';
};

// Bar length expresses progress towards the benchmark, not peer percentile.
// Faster wait times are better, so that metric uses the inverse ratio.
window.GAILS.metricBenchmarkAttainment = function (metric, value, benchmark) {
  if (value === null || value === undefined || isNaN(value) || !benchmark || benchmark <= 0) return 0;
  var ratio = metric === 'at' ? benchmark / Math.max(1, value) : Math.max(0, value) / benchmark;
  return Math.max(0, Math.min(100, ratio * 100));
};

// ========== UTILITY ==========
window.GAILS.bc = function (b) { return String(b || 'No Data').replace(/\s/g, '-'); };
